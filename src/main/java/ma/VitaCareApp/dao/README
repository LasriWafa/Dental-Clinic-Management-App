
dao (Data Access Object):
Manages data persistence and retrieval.
Since you're using a file as your database, this package will include logic to read/write files.

Examples:
PatientDAO.java
AppointmentDAO.java

These classes will:
Provide CRUD operations for each model.
Handle file operations (e.g., JSON, CSV, or plain text).